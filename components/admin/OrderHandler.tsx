import { Text, Title } from "@mantine/core";
import { FC } from "react";

const OrderHandler = () => {
  return (
    <Title color="brand.6" size="sm">
      Orderhanterare
    </Title>
  );
};

export default OrderHandler;
